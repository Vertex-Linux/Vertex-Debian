# live-build build state
/binary/
/chroot/
/cache/
/.build/
/live-image-*.hybrid.iso
auto/config.bak

# live-build's own auto-generated manifests (file/package lists per build —
# huge, regenerated every run, no value as source content)
/chroot.files
/chroot.packages.install
/chroot.packages.live
/chroot.packages.purge
/chroot.packages.remove
/live-image-*.files
/live-image-*.packages
/live-image-*.contents
/live-image-*.buildlog

# build output
/out/
