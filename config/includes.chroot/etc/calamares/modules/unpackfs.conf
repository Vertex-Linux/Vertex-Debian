# Copies the live squashfs filesystem onto the target disk.
# The path must match where live-build places the compressed root
# filesystem on the ISO (live/filesystem.squashfs by default).
---
unpack:
    - source: "/run/live/medium/live/filesystem.squashfs"
      sourcefs: "squashfs"
      destination: ""
